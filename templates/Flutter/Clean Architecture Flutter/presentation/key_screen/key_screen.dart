part of '../../<{key_sc}>.dart';


class <{key}>Screen extends StatelessWidget {
  const <{key}>Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          '<{key}>Screen',
        ),
      ),
    );
  }
}
